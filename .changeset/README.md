# Changesets

Add a changeset for every user-facing fix, feature, or breaking change:

```sh
npm run changeset
```

Documentation-only, test-only, refactoring, and infrastructure changes do not
need a changeset unless they alter the published package.
