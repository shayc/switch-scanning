---
"@shayc/switch-scanning": patch
---

Keep imperative React bindings on committed option and listener values during
concurrent rendering, and type `useScannerSnapshot(scanner)` as a complete
`ScannerSnapshot` when no selector is supplied.
